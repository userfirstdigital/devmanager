# Task 5C Sequential Checkpoints

Source requirements: `browser-task-5c-brief.md`. Start only after Task 5B is fully approved. Every checkpoint uses strict RED-to-GREEN TDD, one implementer, immutable-range spec/quality review, and a separate commit.

Status (2026-07-20): checkpoints 1 through 12 are implementation-complete. Checkpoint 12 now includes the shutdown-liveness fixes through `de332e5`, canceled-generation draining at `4914bf0`, bounded authenticated MCP bodies at `7efb2cd`, bounded workflow repositories and locators at `a7e2f36`, and production replay-state cleanup at `db8f08e`. The final independent review found 0 Critical, 0 Important, and 0 Minor findings. Fresh final-head gates passed: format; warning-free locked all-target check; 788/788 library tests plus every integration, binary, and example target; native Windows release build; and local Windows x64 NSIS/WiX packaging. The exact formerly stuck shutdown path exited in 32 ms in an instrumented release-candidate run. Earlier manual acceptance covered the split pane, Claude/Codex launch and injection, conversation isolation/restoration, tabs, viewport, annotations, recording review, and collapse/reopen. Real-agent invocation of every browser tool and the Windows ARM64/macOS ARM64 CI package jobs remain release-matrix follow-ups rather than locally completed evidence.

1. **Strict recipe wire/store — complete** — strengthened `recipes.rs` with one duplicate-member-rejecting, deny-unknown-fields v1 wire; checked public nested deserialization; typed action/value/wait/assertion structures; reference/type validation; Secret/File default prohibition; redaction gates; deterministic pretty JSON plus newline; contained list/load/save; Windows reparse/operation-boundary checks; bounded owned stale-temp scavenging; and atomic sibling-temp replacement with failure coverage. See `browser-task-5c-report.md`.
2. **Pure recording/review domain — complete** — added a bounded platform-neutral recorder keyed only by `BrowserWorkspaceKey`; inactive by default; exact instance and reserve/commit/cancel source ordering; workspace/restart/late-completion fencing; safe coalescing; content-free generated Secret/File inputs; immutable review metadata, step/input/wait/assertion mutations, strict v1 save handoff, and discard. Independent-review hardening adds repeated percent-decoded query/fragment credential inspection, fail-closed malformed encoding and unresolved generic input references, generated-input provenance with centralized last-reference collection after successful reference-changing mutations, and atomic fixed input/assertion bounds. No store/UI/IPC/MCP/host/replay wiring is present. See `browser-task-5c-report.md`.
3. **Semantic page recording IPC — complete** — added an active-only, bounded, duplicate/unknown-field-rejecting v1 page envelope with exact workspace/tab/revision/origin/instance/nonce and replay fencing; trusted semantic click/type/select/navigation/upload/download conversion; content-free password/credential/clipboard/file markers; a private bounded Windows transport; fresh post-load injection; and Stop/navigation/close/reset/profile-clear teardown. Independent-review hardening adds synchronous fence-before-drain Stop ordering, typed overflow invalidation/discard, pre-capacity stale-instance rejection, bare-token source/Rust defenses without label/element-text locator reads, and one real-URL canonical HTTP(S) origin path. Tab chrome capture remains checkpoint 4 and is not accepted from page JavaScript. See `browser-task-5c-report.md`.
4. **Unified host capture — complete** — added one cloneable platform-neutral coordinator over the exact checkpoint-2 recorder authority; page IPC, user chrome, and queued agent/controller actions now share its workspace/instance/order fence. Prior page IPC drains before host reservations. User create/select/close, navigation/history/reload, and viewport changes preflight a sanitized intent and reserve before browser mutation, then commit a response-derived typed action with deterministic logical tab aliases only after the exact successful Workspace response. Independent-review hardening makes every preflight, capacity, sanitizer, alias, conversion, and commit failure invalidate/discard only the exact incomplete recording while emitting a typed host diagnostic; browser failures cancel normally, and stale completion cannot discard a restarted instance. Agent work reserves before queueing, runtime-inspects action targets before execution/value retention, commits only exact successful response variants, and cancels every failed, denied, interrupted, stale, stopped, or mismatched slot. Password/security typing creates only an unset Secret input; password-target keypress capture permits only the documented fixed non-text key allowlist. Upload stores only an unset File marker and CDP stores only a validated method marker; paths, contents, params, bodies, and results never enter recorder state. See `browser-task-5c-report.md`.
5. **Pane Record/review UI — complete** — added local Claude/Codex-only explicit Record and persistent red Stop Recording controls; Stop-to-review without auto-save; a scrollable native review surface replacing the page canvas; safe metadata/viewport editing; ordered actor-labelled steps; delete/reorder and Text/URL conversion; bounded unset Text/URL/File/Secret input creation plus rename/default/remove; safe wait presets and all five typed assertion kinds with removal; validated immutable preview; exact-instance discard; and deterministic atomic save to the canonical owning repository root. Independent-review hardening keeps invalid drafts projection-editable, accepts user-entered assertion expectations while deriving element/value locators from the real recorded action, wraps all dense controls at the 320px pane minimum, and rejects any reorder involving or crossing tab-lifecycle actions while preserving adjacent non-tab moves. Review/preview/editor state is volatile and route-owned, failed saves retain it, remote/Server/SSH surfaces expose no controls, and checkpoints 6 through 12 remain untouched. See `browser-task-5c-report.md`.
6. **Exact `browser_recording` MCP — complete** — added the sole exact `status|start|stop|review|discard|save` group with a bounded nonblank intent and exact risk enum; bearer-bound workspace/root routing; one coordinator lifecycle; compact value-free inline state and owner-scoped validated review resources; runtime overwrite/discard risk escalation and approval; exact-instance stale fencing; hardened no-clobber/overwrite atomic save; failure retention; safe journaling; and typed unsupported-platform behavior. See `browser-task-5c-report.md`.
7. **Replay compiler/status/cancellation lease — complete** — added a strict platform-neutral `BrowserRecipeV1` compiler with exact bounded public Text/URL/File inputs, declaration-ordered immutable non-secret bindings, safe Text/URL defaults, names-only unresolved Secrets, and non-`Debug`/non-`Serialize` value carriers. One mutex-backed coordinator owns exact opaque-scoped workspace/instance fencing, the seven closed statuses, explicit legal transitions, ordered step progress, typed failures, explicit replacement, terminal immutability, safe bounded oldest-first cleanup, and one non-rearming replay-lifetime cancellation authority shared by every lease clone. Cancel, replacement, and workspace interruption synchronously invalidate it across Pending, NeedsUserSecret, Running gaps, and locator-repair pause. Independent-review hardening makes recipe and every step identifier share one syntax-plus-nontruncating-credential predicate across validation, direct/nested serialization, deserialization, and repository ID paths, so credential-shaped slugs cannot enter a plan or value-free projection/history. This checkpoint executes nothing and has no host/controller/queue/approval/journal/filesystem/UI/MCP/secret-value/repair-payload integration. See `browser-task-5c-report.md`.
8. **Replay via existing queue/approval/journal — complete** — verifies the authenticated canonical root before browser effects; creates a fresh setup tab, applies viewport/start URL, and maps deterministic portable aliases; then awaits exactly one existing controller command at a time for every action, optional typed wait, and ordered assertion. Existing actor contexts, operation queue, runtime target inspection, approval, cancellation, and journal paths remain authoritative. Runtime snapshots and exact response variants gate progress; first failure stops later work. Upload candidates are canonicalized at execution time with symlink/reparse containment and `OutsideWorkspaceFile` risk. Cancellation, replacement, interruption, and late responses cannot mutate aliases or advance the old instance.
9. **Memory-only secrets — complete, frozen for independent review** — adds one exact-instance zeroizing replay store; a value-free `SecretType` command plus private non-Debug/non-serde sidecar; Windows queue, target inspection, approval, recording, journal, taint, native-navigation, and callback fencing; executor-only Secret `Type` routing; and a NativeShell-owned fixed-mask prompt whose plaintext never enters pane/persisted/remote state. Review hardening closes input-capacity, cross-replay authority, missing-document-state, asynchronous exposure, source-order recorder ownership, prompt-allocation, and annotation-focus boundaries. MCP has no secret-submission wire. Final evidence is 168/168 focused tests, 153/153 aggregate browser matches, locked all-target check, native Windows release build, format, diff, and explicit leakage scans. The independently approved fixture-byte prerequisite `429dbc8` is outside the checkpoint artifact. Checkpoint 9 itself did not include locator repair or the exact `browser_workflow` MCP/lifecycle.
10. **Typed locator failure/repair state — complete and independently approved** — adds fixed provenance-authenticated `LocatorNotFound` target kinds; exact opaque repair identity; one live-runtime, owner-scoped nonpersistent evidence-retention lease; dedicated semantic-snapshot/screenshot host sidecars and resource kinds; value-free paused projection/watch state; exact locator-slot and resume-cursor mapping; fresh ordered evidence capture; and a stable executor pause across early signal, sender drop, cancel, replacement, interruption, and late-response races. Independent review caught a test-only same-replay stale-generation resume seam; the strict regression requires exact active paused repair equality before any take or signal. Final checkpoint-10 evidence is 219/219 focused tests, 187/187 corrected single-job aggregate browser matches, locked all-target check, format/diff/leakage audits, and the approved checkpoint-11 base `6972adb`.
11. **Repair preview/atomic apply — complete; frozen for independent review** — current-revision User/Agent semantic candidates install an exact token-owned, pointer-transparent highlight through the existing queue/journal without page input, revision, or recording effects. Explicit confirmation and the existing approval policy apply a Destructive Agent floor while preserving higher risk. A domain-separated canonical recipe digest, one exact authenticated-root binding, exact recipe/step/slot/old-locator comparison, full validation, the shared recipe write gate, and atomic sibling replacement preserve a complete old or new file for cooperating DevManager writers. One coordinator reservation fences cancellation/replacement/interruption and commits file+runtime override+`Applied` state coherently; post-write page drift remains safely Applied. Action/action-wait/step-wait/assertion cursors retry only the failed phase, never duplicate a successful mutation, and clean every repair generation/resource lease. Review remediation moved fallible post-validation context creation before durable commit and made private `RepairValidate` unconstructible outside its sealed internal lane. A separate one-file test correction now permits only the exact canonical-root `Path`/`PathBuf` coupling while continuing to reject all other platform/execution coupling. Task-8 verification and artifact identity are recorded in `browser-task-5c-report.md`.
12. **Exact `browser_workflow` MCP/lifecycle — complete and independently approved** — exposes only `list|get|replay|status|cancel|repairPreview|repairApply` with strict per-operation fields, exact risk enums, bounded public Text/URL/File inputs, compact value-free replay/repair projections, and owner-scoped recipe/evidence resources. A fresh 256-bit bearer registration binds one process, exact `{ project_id, ai_tab_id }` workspace, lease, and canonical local root; stale or cross-workspace credentials/resources fail closed. Secret values have no MCP wire and remain solely in the NativeShell-owned zeroizing masked vault. Repair requires exact current-revision preview followed by confirmed save-only or save-and-retry apply. One linearized controller bridge owns Stop/direct-input/tab-close/workspace-switch/reset/profile-clear/provider-revocation/process-loss/settings-disable/shutdown cancellation at the documented tab, workspace, project, registration, or all-local scope and fences late responses. Claude gets an ephemeral MCP overlay, Codex gets session-only MCP overrides, and any injection/gateway/WebView failure preserves the original terminal launch with a diagnostic. Windows uses Wry/WebView2; macOS/non-Windows compiles an unavailable adapter with no partial WebKit host. No whole-PC control, Playwright browser backend, Node sidecar, external-Chrome mode, desktop-control surface, or second replay owner is added. Final hardening moves WebView teardown out of the closing window-owned pump, drains canceled native generations before reopen, caps authenticated MCP request bodies at 1 MiB, caps workflow files/entries/recipes/inputs/steps/assertions and locator content, and removes a production-only duplicate replay cursor field. The final review found no findings, and fresh final-head verification passed format, warning-free locked all-target check, 788/788 library tests plus all other targets, native release build, and local Windows x64 NSIS/WiX packaging. The formerly stuck shutdown path exited in 32 ms under live instrumentation; the clean uninstrumented rerun could launch but could not be controlled after Windows Computer Use twice returned `foreground window did not report a process id`, so no clean rerun result is inferred. Cross-architecture CI packaging and exhaustive real-provider tool invocation remain explicit release-acceptance follow-ups.

High-risk review gates: no secret ever enters serializable/debuggable action state; cancellation works between controller calls; Windows atomic replacement leaves old or new file; async recorder ordering; workspace-switch cancellation; repair cannot apply against changed recipe/page/workspace.
