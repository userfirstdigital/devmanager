# DevManager Per-Conversation Browser V1 (2026-07-16)

Execution location: clean `master`, explicitly authorized by the user.
Task 1: complete - clean baseline (`cargo build --locked`; 545 library tests plus integration suites green) and browser subsystem interfaces locked.
Task 2: complete - browser domain, persistence, security, storage layout, annotations, and recipes (`9ebb1dd`, secret-serialization review fix `fff9fd5`; task review approved).
Task 3: complete - Windows WebView2 host (`a5c5072`, wake/cancellation fix `3f851b5`) plus GPUI split companion pane (`ae31d3f`). Focused review found restored-open panes did not recreate their host workspace; TDD fix `fd4d16a` ensures only absent host state and preserves newer live state. Re-review approved. The large host slice still receives a focused check in the whole-branch review because earlier independent host reviewers stalled on the generated package.
Task 4: complete - authenticated rmcp gateway and Claude/Codex session injection (`25f83f2`). Parallel security/process review found startup readiness, bounded shutdown, terminal-exit/write-failure cleanup, Cmd path quoting, live MCP state, and shutdown/register race gaps; strict TDD hardening `d2eea73` passed browser 57/57, ProcessManager 62/62, Codex 32/32, all-targets check/build, and both re-reviews approved.
Task 5: complete through Task 5C checkpoint 12 and final hardening at `db8f08e`. Task 5A and Task 5B implementation remain present with their detailed checkpoint evidence below. Task 5C includes the strict `browser_workflow` MCP, owner/root-bound resources, native-only zeroizing Secret handoff, repair controls, linearized lifecycle cancellation, provider injection/fallback, Windows Wry/WebView2 support, and the typed non-Windows unavailable adapter. The final shutdown fix completes WebView teardown from the app executor instead of the closing window-owned pump; follow-ups drain canceled generations before reopen, bound authenticated MCP requests and recipe/locator inputs, and remove test-only cursor state from production. Final independent review found 0 Critical, 0 Important, and 0 Minor findings. Fresh final-head format, warning-free locked all-target check, 788/788 library tests plus all other targets, native release build, and local Windows x64 NSIS/WiX packaging are green. The exact formerly stuck shutdown path exited in 32 ms under live instrumentation.
Task 5C checkpoint 3 independent-review hardening is complete under strict RED-to-GREEN: Stop fences before draining accepted source-order messages, overflow explicitly invalidates/discards only its exact incomplete instance, stale traffic cannot consume restart capacity, bare JWT/API-key credentials are content-free at the source and rejected in Rust, locator capture avoids label/element text, and one real-URL canonical origin path covers the authority/script/request boundary. Follow-up gates cover 2 transport unit tests, 104 focused host/recording/IPC tests, 208 full browser integration tests, 109 aggregate browser matches, all-target check, and native Windows build. At that checkpoint head, checkpoints 4 through 12 were untouched.
Task 5C checkpoint 5 Pane Record/review UI is complete from approved head `59846cb`, with the initial implementation at `109530a` and a strict-TDD independent-review hardening follow-up: local Claude/Codex panes expose explicit Record, red active Stop Recording, and a scrollable native review surface with value-free User/Agent step projection, repairable invalid metadata drafts, user-entered assertion expectations, real recorded-action locators for element/value assertions, tab-lifecycle-safe reordering, wrapped 320px-reachable controls, bounded typed inputs, waits, validated preview, exact discard, and deterministic atomic save to the canonical owning repository. Review/editor/preview state is volatile and route-owned; remote/Server/SSH surfaces expose no workflow controls; failed saves retain review; destructive lifecycle discards exact Recording-or-Review state.
Task 5C checkpoint 6 exact recording MCP is complete through its strict-TDD independent-review hardening follow-up, with the initial implementation at `8d0d8a2`: one strict `browser_recording` group exposes only `status|start|stop|review|discard|save` with bounded nonblank intent and exact risk, while bearer registration supplies the sole workspace and canonical local root. One coordinator owns lifecycle; inline results are compact/value-free; full validated recipes are owner-scoped resources; new save is Normal while overwrite/discard escalate through the existing approval queue; approval resume fences the original instance; no-clobber and overwrite saves use hardened atomic repository IO; failures retain Review; success/discard retire exactly; journals retain safe operation metadata/resource IDs; and unsupported platforms return typed unavailable errors. Review hardening makes every Stop/Review resource-store failure one fixed path-free typed result while retaining Review, rejects noncanonical or UNC/remote roots before any of the six operations can initialize or mutate lifecycle state, and queues Save/Discard plus approval under one stable workspace target independent of selected-tab changes. At the checkpoint-6 head, checkpoints 7 through 12 were pending and no replay, runtime secret prompt, locator repair, or `browser_workflow` MCP had started.
Task 5C checkpoint 7 replay domain is complete under strict RED-to-GREEN from approved base `0f35ff6`: a pure strict compiler accepts only exact bounded public non-secret inputs, preserves declaration-ordered immutable steps/bindings, applies safe Text/URL defaults, and leaves only safe Secret names unresolved. Non-Debug/non-Serialize plans and replay-lifetime leases keep values off diagnostics and wires; safe projections expose exactly seven statuses plus closed failure codes. One cloneable platform-neutral coordinator enforces opaque-scoped exact workspace/instance fencing, explicit legal transitions and step order, one active replay per workspace, replacement/late/terminal rejection, checked monotonic IDs, poison recovery, and bounded oldest-first safe terminal cleanup. One immutable shared cancellation authority spans Pending, NeedsUserSecret, Running gaps, and locator pause without rearming; cancel, replacement, and workspace interruption synchronously invalidate every clone. Independent-review hardening rejects bare credential-shaped recipe and step IDs through one centralized syntax-plus-nontruncating-content predicate at validation and every direct/nested wire boundary before replay plan/start, preventing projection/history disclosure while preserving ordinary IDs. Checkpoints 8 through 12 remain pending; there is no replay execution, host/controller/queue/approval/journal/filesystem/UI/MCP wiring, runtime secret value, or locator-repair payload.
Task 5C checkpoint 8 replay execution is complete under strict RED-to-GREEN from approved base `c57cfd6`; implementation and the public runbook are frozen at `29291e6`, followed only by the final evidence commit used as the independent-review head: an exact non-debuggable execution handle drives a fresh setup tab, recipe viewport/start URL, portable tab aliases, every strict action, bounded typed waits, and ordered assertions through one existing `BrowserController` request at a time. Existing operation queue, runtime target inspection, approval, cancellation, resources, and agent journal remain authoritative. Exact response/snapshot proofs gate progress; first failure stops later work; ordinary versus assertion mismatch produces closed distinct failure codes. Upload File inputs resolve and canonicalize at execution time with symlink/reparse containment and exact `OutsideWorkspaceFile` approval risk. Cancellation, replacement, interruption, and late responses cannot mutate aliases or advance the old exact instance, and host/path/value details never enter new Debug/Serialize status/error surfaces. Focused checkpoint gates passed 159/159, aggregate browser 122/122, ProcessManager 70/70, locked all-target check, native Windows release build, format, and exact diff checks. Checkpoints 9 through 12 remain pending; no runtime secret values, locator repair, workflow MCP, or lifecycle UI has started.
Task 5C checkpoint 9 memory-only replay secrets is complete and frozen for independent review from approved checkpoint-8 base `7f4afb3`: Tasks 1-3 are independently approved through third-remediation commit `27e9c2a`, with exact zeroizing stores/private sidecars, sealed Windows typing, pre-exposure taint, native navigation-ID fencing, fail-closed missing-state draining, an idempotent in-flight exposure lease, and transactional source-order Secret/File recording ownership. Task 4 is independently approved through `4d6123b`: the executor routes only Secret-kind `Type` through the private lane, while NativeShell alone owns preallocated zeroizing prompt values and projects only exact-instance names, focus, `is_set`, and a constant eight-bullet mask; prompt input owns focus ahead of annotation, clipboard, terminal, composer, and remote paths. Task 5 gates passed 168/168 focused tests and 153/153 aggregate browser matches plus locked all-target check, native Windows release build, format, diff, compile-time trait, serialized/Debug/MCP/resource/journal/recording, tracked-source, and release-artifact leakage checks. The independently approved fixture-byte prerequisite `429dbc8` is separate from and excluded from the checkpoint-9 review artifact. No Critical/Important/Minor Task-4 finding remains; theoretical exposure-counter saturation after approximately `2^64` transitions remains a Task-3 Minor final-review note. Checkpoints 10-12 are pending; locator repair and exact `browser_workflow` MCP/lifecycle have not started.
Task 5C checkpoint 10 typed locator failure/evidence/stable-pause is complete and independently approved at checkpoint-11 base `6972adb`. The exact lineage adds provenance-authenticated fixed `LocatorNotFound` target kinds, one live-runtime nonpersistent evidence lease, exact coordinator repair state, dedicated owner-scoped Windows snapshot/screenshot sidecars, ordered fresh executor capture, and a stable value-free pause across cancellation/replacement/interruption/sender-drop/late-response races. Independent review's stale same-replay generation finding is fixed: test-only resume requires exact active paused `BrowserReplayRepairInstance` equality before take/signal. Final focused gates passed 219/219; the corrected one-Cargo-job aggregate passed 187/187 browser matches; locked all-target check passed. Explicit projection/error/journal/Debug/serde/source scans found no selector, page text, path, input value, secret, or callback payload on safe repair surfaces, and the checkpoint secret sentinel occurs only inside a `#[cfg(test)]` executor module. Task-4 evidence was committed at `e541996`; its immutable review completed before checkpoint 11 began.
Task 5C checkpoint 11 locator preview/atomic apply/same-step resume is complete from approved base `6972adb` through preview `124769f`, digest/atomic exact-locator update `594030e`, and confirmation/resume `d9707c2`, followed by the separate scope-test correction `f50cdf6`. Current-revision User/Agent candidates use an exact owned highlight token with no page-input/revision/recording side effect; evidence and cleanup stay owner-scoped. Explicit confirmation, a Destructive Agent approval floor, exact pre/post validation, and one coordinator reservation fence changed page/recipe/workspace/replay/cancellation races. Apply reuses the domain-separated canonical recipe digest, authenticated-root binding, shared recipe write gate, full revalidation, and atomic sibling replacement; cooperating DevManager writers retain a complete old or new recipe, while non-cooperating external-editor CAS and Windows crash-durability equivalence are not claimed. File+runtime override+`Applied` state commit coherently, post-write drift remains Applied, and fresh exact preview is required for later no-write resume. Action/action-wait/step-wait/assertion cursors retry only the failed phase, never duplicate a successful mutation, and second-generation repair cancellation releases all evidence. Review hardening moves fallible post-context construction before durable commit and seals the private non-serde `RepairValidate` command against external construction. The separate `f50cdf6` test fix allowlists only the authenticated canonical-root `Path`/`PathBuf` coupling and rejects any other `std::path` or execution/platform surface. Task-8 verification is recorded in `browser-task-5c-report.md`.
Task 5C checkpoint 12 exact workflow MCP/native/provider lifecycle is complete and independently approved through `db8f08e`. `browser_workflow` exposes exactly `list|get|replay|status|cancel|repairPreview|repairApply` with strict operation fields, exact risks, bounded Text/URL/File public values, compact value-free projections, and owner-scoped resources behind the bearer-bound exact workspace and canonical local root. Secret plaintext has no MCP wire and remains only in the NativeShell zeroizing vault. Preview/apply uses exact revision and explicit confirmation/resume, while Stop, direct input, tab/workspace/project/provider/process/settings/app lifecycle transitions cancel at their exact ownership scopes before mutation and fence late work. Claude receives an ephemeral overlay; Codex receives session-only overrides; failures preserve the original terminal launch. Windows uses Wry/WebView2 and macOS/non-Windows uses the unavailable adapter. No whole-PC, Playwright backend, Node sidecar, external Chrome, desktop-control surface, partial WebKit host, or second replay owner is included. Shutdown and resource-bound hardening closes the final runtime/review findings, and final-head verification is green.
Task 6: locally complete - the feature release is versioned `0.4.0`; format, warning-free locked all-target check, 788/788 library tests plus all integration/binary/example targets, native Windows release build, Windows x64 NSIS/WiX packages, Windows executable file/product metadata, artifact hashes, and native companion-pane acceptance are recorded. Windows ARM64/macOS ARM64 CI packaging and exhaustive real-agent invocation of every browser tool remain release-matrix follow-ups and are not claimed as local evidence. The exact stuck shutdown path exited in 32 ms under instrumentation; the clean retry was inconclusive because Windows Computer Use could not bind to the isolated candidate window after one refresh/retry.
Task 7: complete - final focused review found no Critical, Important, or Minor findings; the implementation is ready for handoff with the explicit release-matrix/manual coverage limits recorded above.

# Prior SSH Feature Ledger

Task 1: complete (commits 70adf20..bddf7af, review clean)
Task 2 minors (for final review triage): (a) safe_key_file_name("") unguarded -> dir.join("") writes to dir path, unhelpful IO error; (b) no test for lone-\r input to sanitize_private_key.
Task 2: complete (commits bddf7af..9204e83 incl. icacls-path fix, review clean)
Task 3: complete (commits 9204e83..0ee7f3e, review clean)
Task 3 minors (triage): (a) spawn_ssh_session early-return could someday skip respawn while key_error notice still writes; (b) raw materialize error string unsanitized in terminal notice.
Task 4: complete (commits 0ee7f3e..7fb0d52 incl. local-only cleanup fix, review clean)
Task 4 note (triage): host-side RemoteAction::SaveSsh apply path does not clean stale key file when key cleared remotely — accepted best-effort gap per spec.
Task 5: complete (commits 7fb0d52..66b716c, review clean)
Task 5 minor (triage): terminal_text_bounds gate change (line ~9685) was required compile fix, verified consistent with terminal_dimensions:3549 precedent; no test coverage for it.
Task 6: complete (3x full suite green, clippy clean; final whole-branch review "with fixes"; fix batch e798670 + docs 9d09825; re-review: ready for merge preparation)
Wire-exposure decision (user, 2026-07-10): accept + document; redaction w/ keep-sentinel folded into encryption-at-rest follow-up.
Outstanding before merge: interactive manual QA (7 steps from final review) — requires user's environment.
Browser checkpoint 9 Task 1: complete (commits cac4ae8..11937fb, independent spec and quality review approved; exact zeroizing replay secret store and compiler capacity boundary).
Browser checkpoint 9 Task 2: complete (commits df43754..aba7ea1, independent spec and security re-review approved; private sidecar is bound to the exact opaque replay instance and revalidated at ingress and approval resume).
Known pre-existing flake (NOT this branch): remote::tests::native_client_connections_are_recorded_in_activity_log (src/remote/mod.rs:5031) — access-log persist race, reproduced 1/5 runs; src/remote/ and src/persistence/ untouched by branch (git diff empty). Suggest follow-up to deflake.

# Native LLM Chat Perfection Progress

Baseline: `7607efc feat(web): make remote sessions live-first`
Plan: `docs/superpowers/plans/2026-07-21-native-llm-chat-perfection.md`

Task 1: complete — current Codex hook overrides use shell-safe Windows paths without changing POSIX paths; focused Rust tests pass and independent review is clean.

Task 2: complete — AI fallback uses a replacing post-parse screen projection, preserves visible indentation, clears stale frames, and leaves non-AI/raw streaming unchanged; focused Rust tests pass and independent review is clean (empty-card rendering is assigned to Task 3).

Task 3: complete — native chat groups a turn into one compact activity disclosure, exposes Stop only while thinking, keeps drafts editable across reconnects, preserves unsent text around one-shot question choices, and prefers stable semantic titles; 325 web tests, typecheck, production build, and independent review are clean.

Task 4: complete — native Windows Codex uses the supported PowerShell `command_windows` hook with an explicit call operator, enabling SessionStart, prompt, tool, response, and Stop semantics on Codex CLI 0.145.0. The degraded screen projector suppresses startup commands and relay nonces, and the web UI shows fallback guidance only when fallback text is actually visible.

Acceptance: complete — phone-size hot reload passed fresh Claude and Codex conversations, compact multi-tool activity, exact streamed replies, unsent-draft return, native empty state, and zero hook-failure/fallback/nonce leakage in the final Codex run. Final gates: 325/325 web tests, web typecheck and production/PWA bundle, 830/830 runnable Rust library tests (1 explicit real-network probe ignored), formatting, locked all-target checks, diff checks, and a clean independent final review.
