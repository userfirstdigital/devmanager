# Task 5.2 token integration correction report

Date: 2026-08-09

Base: `bae05ab` (`fix(ui): render isolated native preview`)

Cherry-pick: `549fd24` was cherry-picked and resolved as `496eee8`. The
resolution retains the Task 5.1 preview module, adds the single library-owned
`ui::tokens` module, and keeps the checked-in preview fixture compatible with
Task 5.1's strict `deny_unknown_fields` loader.

## TDD evidence

The original commit's focused test run was RED because the test compiled a
private `src/ui` module and `preview.rs` could not resolve the library's
`assets`, `client`, and `terminal` modules: 3 unresolved-import errors.

After moving token imports to `devmanager::ui::tokens` and adding the reviewed
contract tests, the required pre-correction RED was captured before the
production correction: 5 compile errors for the intentionally missing
interaction-state and status-surface contrast APIs.

Focused GREEN:

```text
cargo test --test ui_tokens -- --nocapture
12 passed; 0 failed
```

Relevant projection GREEN:

```text
cargo test --test ui_projection -- --nocapture
12 passed; 0 failed; 1 ignored
```

The ignored test is the concrete native PNG acceptance test, with the reason
that GPUI 0.2.2 has no official isolated pixel readback or PNG encoder.

## Second independent review correction

The second review identified four independent gaps: action-state backgrounds
and borders were not part of the 3:1 UI-indicator contract; terminal ANSI
foreground capability was implicit; the coverage matrix was generated by the
test rather than loaded from independent typed data; and the direct-literal
scan exempted token filenames instead of the one canonical token module.

Strict TDD RED was captured before the correction with the unique target
directory `C:\Temp\devmanager-task52-second-review-red-20260809`:

```text
cargo test --test ui_tokens -- --nocapture
12 passed; 5 failed
```

The five failing tests covered the action-state pair contract, the missing
action pair inventory, terminal-slot inventory, canonical token-module
exemption, and independent matrix fixture. The first action-state diagnostic
reported `2.818:1` for the primary default background on canvas.

The correction adds all 20 primary/destructive background and border pairs,
classifies all 20 terminal slots (17 foreground-capable and 3 structural),
loads all 16 explicit theme/density/scale cases from the typed
`theme-matrix.json` fixture, and requires exactly one production
`src/ui/tokens.rs` module. The new cockpit fills were tuned only in the new
token palette; legacy terminal and `theme::*` aliases were not changed.

Focused GREEN after the correction:

```text
cargo test --test ui_tokens -- --nocapture
19 passed; 0 failed
```

The exhaustive diagnostic's minimum ratios across all 16 matrix cases are:

| Contract | Minimum | Pair |
| --- | ---: | --- |
| Normal text | 5.028:1 | `terminal_magenta_on_background` |
| Large text | 13.963:1 | `text_primary_large_on_canvas` |
| UI indicator | 3.039:1 | `action_primary_default_background_on_canvas` |
| Disabled text | 5.104:1 | `text_disabled_on_selection` |
| Interaction text | 5.117:1 | `action_primary_selected_on_surface` |
| Status surface | 8.704:1 | `status_success_surface` |

The 17 terminal slots rendered as normal foreground text are each checked at
4.5:1 against the terminal background in both themes. Background, cursor, and
selection slots are explicitly typed and excluded from that foreground
contract. The action background and border pairs are each checked at 3:1
against their owning canvas surface in both themes.

## Corrections

- Exposed semantic colors now include action primary/destructive states and
  truthful foreground/background status surfaces.
- Normal text, including disabled text, is checked at 4.5:1. The 3:1
  threshold remains limited to large text and UI indicators.
- Interaction contrast pairs explicitly declare default, hover, focus,
  selected, and disabled for both action families; all six status lights have
  explicit surface and indicator pair declarations.
- Legacy `theme::*` constants retain their prior values, including the
  indigo primary and the less-common editor/selection colors, until cutover.
- The native UI source scan rejects actual numeric/hex color literals while
  allowing token-to-`rgb(...)` conversion calls. Only the exact canonical
  `src/ui/tokens.rs` path is exempted, and the test asserts there is exactly
  one production token module.
- The token test matrix binds long labels, Unicode, disabled content, themes,
  densities, scales, interaction states, and all six status lights across the
  16 explicit cases in the independent typed fixture. The strict Task 5.1
  `theme-gallery.json` schema remains unchanged.

The checked-in `theme-gallery.json` remains the strict Task 5.1 preview
fixture. Its Task 5.2 matrix is kept as deterministic scoped test data because
the current preview parser rejects extension fields; weakening or changing the
preview renderer was outside this task's allowed files.

## Verification

All Rust commands used an isolated target directory. The original correction
used `C:\Temp\devmanager-task52-red-20260809`; the second-review correction
used `C:\Temp\devmanager-task52-second-review-red-20260809`.

- `cargo check --locked --bin devmanager-next`: exit 0.
- `cargo fmt --all -- --check`: exit 0.
- `git diff --check`: exit 0.
- `cargo test --test ui_projection -- --nocapture`: 12 passed, 0 failed,
  1 ignored (the blocked PNG gate).
- Exact native UI scan: 0 direct numeric/hex color hits outside the canonical
  token path; exactly one production token module.
- Scoped worktree/target process check: no Cargo, rustc, or test-harness
  process remained for this worktree. Concurrent processes owned by other
  worktrees were left untouched.
- Preview CLI: exit 2 with the documented GPUI headless readback error;
  `theme-gallery-task52-20260809.png` was not created.

## Remaining blocker

Task 5.2's visual matrix is not complete. The pinned GPUI 0.2.2 stack still
cannot emit a real isolated native PNG, so no screenshot is claimed or
fabricated. The visual gate remains blocked until an approved capture/readback
capability or explicitly authorized dependency change exists. No reviewer was
launched.

No production AppData/session file, installed DevManager, legacy app/chrome,
preview renderer, Cargo manifest/lockfile, merge, push, install, or unrelated
source path was changed.
