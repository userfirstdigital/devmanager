# Phase 7 Task 7.2 report: readable bounded prompt-version comparison

Date: 2026-08-09
Worktree: `C:\Code\userfirst\devmanager\.worktrees\phase-7-2-prompt-diff`
Branch: `codex/phase-7-2-prompt-diff`
Base: `4c94ebe`

## RED

Before adding the implementation or dependency, this exact focused command was
run with `CARGO_TARGET_DIR=C:\Temp\devmanager-phase72-20260809`:

```text
cargo test --test prompt_diff -- --nocapture
```

It failed at compilation with unresolved imports for the missing
`prompts::diff` API and diff types. No prompt-diff tests ran.

## GREEN

The implementation now provides borrowed, byte-preserving `PromptDiff` values,
typed line hunks, grapheme-indexed Unicode inline spans, CRLF/LF comparison
normalization, input-bound validation, cooperative cancellation/deadline
budgets, deterministic hash ordering, and streaming output accounting.

The final focused verification command is:

```text
cargo test --locked --offline --test prompt_diff -- --nocapture
```

Result: 13 passed, 0 failed. Coverage includes identical, empty,
add/remove/replace/move-like content, CRLF and missing final newlines,
combining marks, emoji, CJK, long lines, the 256 KiB input boundary,
pathological many-line output bounds, cancellation/deadline, deterministic
repetition, hash-key ordering, and unchanged input bodies.

Additional gates:

- `cargo check --locked --offline --lib`: passed; only the repository's seven
  pre-existing warnings were emitted.
- `rustfmt --edition 2021` on the three owned Rust files: passed.
- `git diff --cached --check`: passed.
- Locked metadata verified `similar` 3.1.2, Apache-2.0, registry source, and
  Rust 1.85. The registry manifest and packaged LICENSE were checked directly;
  the exact lock checksum and enabled feature selection are recorded in
  `THIRD_PARTY_NOTICES.md`.

## Commit and owned files

Commit message: `feat(prompts): add version diffs`.

Owned files changed:

- `Cargo.toml`
- `Cargo.lock`
- `THIRD_PARTY_NOTICES.md`
- `src/prompts/diff.rs`
- `src/prompts/mod.rs`
- `tests/prompt_diff.rs`
- this Task 7.2 report

No prompt model/store/schema/history/chains/UI/provider-input files, AppData,
`session.json`, or installed DevManager files were changed. The full test
suite was intentionally not run.

## Remaining UI integration blocker

The pure diff API is not wired into prompt history or UI/provider input in this
slice. A later UI task must dispatch the bounded operation from a host
background worker, retain the immutable version bodies, and render the typed
hunks/spans plus truncation or cancellation status. Task 7.1 remains subject
to its independent review as planned.
