# Phase 7 Task 7.2 report: bounded prompt-diff correction

Date: 2026-08-09
Worktree: `C:\Code\userfirst\devmanager\.worktrees\phase-7-2-prompt-diff`
Branch: `codex/phase-7-2-prompt-diff`
Base: `618111b`

## RED

After adding the focused regression assertions and before changing production
code, the required command was run with the isolated target directory:

```text
$env:CARGO_TARGET_DIR = 'C:\Temp\devmanager-phase72-final'
cargo test --locked --offline --test prompt_diff -- --nocapture
```

The test-first run failed at compilation for the intended missing API contract:

- `encode_public_diff` was not yet defined in production;
- callers still saw `DiffCacheKey` as non-optional and could not use the new
  cacheability assertions.

The RED assertions covered the body-free canonical projection and escaped
control-heavy payload cap, invalid oversized input metadata, duplicate-tail
prevention after a partial WorkLimit append, combined approximation plus
payload truncation truth, and non-cacheable invalid results.

## GREEN

The correction now:

- exposes one production `encode_public_diff` JSON projection that omits both
  borrowed source bodies and rejects an actually oversized encoded result with
  a typed error;
- bases output admission on the body-free canonical projection, including JSON
  escaping and combined marker/status overhead;
- settles immediately when WorkLimit interrupts a partially appended mismatch,
  so the tail is not appended a second time;
- reports `ApproximateAndTruncated` with a combined truncation reason when both
  bounded approximation and output truncation are true;
- makes `PromptDiff.cache_key` optional, with `None` for invalid, cancelled, or
  otherwise unhashed results and `Some` only after both bounded hashes finish.

Focused verification:

```text
cargo test --locked --offline --test prompt_diff -- --nocapture
```

Result: 23 passed, 0 failed, 0 ignored. The run emitted only the repository's
seven pre-existing warnings.

Additional gates:

- `rustfmt --edition 2021 src/prompts/diff.rs tests/prompt_diff.rs`: passed;
- final `rustfmt --check` and `git diff --check`: passed;
- the scoped residue check found no Cargo, rustc, or prompt-diff test harness
  process tied to this worktree or `C:\Temp\devmanager-phase72-final`.

## Commit and owned files

Commit message: `fix(prompts): harden bounded prompt diff corrections`.

Owned files changed:

- `src/prompts/diff.rs`;
- `tests/prompt_diff.rs`;
- this Task 7.2 report.

No Cargo or notice change was required. No prompt model/store/schema/history/
chains/UI/provider-input files, AppData, `session.json`, installed DevManager
files, worker, or LRU integration were changed. The full test suite was not
run, and no UI integration, push, merge, install, tag, or publish action was
taken.

## Remaining UI integration blocker

The pure diff API is not wired into prompt history or UI/provider input in this
slice. A later UI task must dispatch the bounded operation from a host
background worker, retain the immutable version bodies, and render the typed
hunks/spans plus truncation or cancellation status. Task 7.1 remains subject
to its independent review as planned.
