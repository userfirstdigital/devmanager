# Phase 7 Task 7.2 report: bounded prompt-diff correction

Date: 2026-08-09
Worktree: `C:\Code\userfirst\devmanager\.worktrees\phase-7-2-prompt-diff`
Branch: `codex/phase-7-2-prompt-diff`
Base: `4c94ebe`

## RED

The pre-correction focused baseline ran with
`CARGO_TARGET_DIR=C:\Temp\devmanager-phase72-correction`:

```text
cargo test --locked --offline --test prompt_diff -- --nocapture
```

It passed 19 tests. After adding the four focused regression assertions and
before changing production code, the same command ran 21 tests: 19 passed and
2 failed for the intended existing defects:

- a 33-line shared anchor outside the 32-line search window returned
  `Complete` instead of `Approximate`;
- the returned escape-heavy public JSON projection encoded to 2,573,413 bytes,
  above the 2,097,152-byte cap.

## GREEN

The correction now marks limited-anchor fallback regions approximate whenever
the searched window cannot prove exact alignment, accounts for JSON string
escaping plus object/array/field and truncation-marker overhead, and removes
the unbounded public `DiffCacheKey::from_bodies` hashing bypass. Public cache
keys are produced only after input validation and budgeted hashing in the diff
operation. Deterministic mid-operation cancellation remains covered after
hashing and line output, in addition to the expired-deadline case.

The focused verification command is:

```text
cargo test --locked --offline --test prompt_diff -- --nocapture
```

Result: 21 passed, 0 failed. Coverage includes the 33-line out-of-window
anchor, escape-heavy Unicode/quote/backslash payload encoding, the 256 KiB
input boundary, deterministic mid-operation cancellation after work has
started, expired deadlines, output-cap precedence over the inline-span cap,
and the existing line/Unicode/CRLF/hash determinism cases.

Additional gates:

- Focused Cargo compilation emitted only the repository's seven pre-existing
  warnings.
- `rustfmt --edition 2021 src/prompts/diff.rs tests/prompt_diff.rs`: passed.
- `git diff --check`: passed.
- Locked metadata verified `similar` 3.1.2, Apache-2.0, registry source, and
  Rust 1.85. The registry manifest and packaged LICENSE were checked directly;
  the exact lock checksum and enabled feature selection are recorded in
  `THIRD_PARTY_NOTICES.md`.

## Commit and owned files

Commit message: `fix(prompts): harden bounded prompt diffs`.

Owned files changed:

- `Cargo.toml` and `Cargo.lock` (existing Phase 7.2 dependency slice)
- `src/prompts/diff.rs`
- `tests/prompt_diff.rs`
- this Task 7.2 report

No prompt model/store/schema/history/chains/UI/provider-input files, AppData,
`session.json`, installed DevManager files, worker, or LRU integration were
changed. The full test suite was intentionally not run. The process check found
no Cargo/rustc/test-harness process tied to this worktree or
`C:\Temp\devmanager-phase72-correction`; unrelated Cargo/rustc processes from
other active worktrees were left untouched.

## Remaining UI integration blocker

The pure diff API is not wired into prompt history or UI/provider input in this
slice. A later UI task must dispatch the bounded operation from a host
background worker, retain the immutable version bodies, and render the typed
hunks/spans plus truncation or cancellation status. Task 7.1 remains subject
to its independent review as planned.
