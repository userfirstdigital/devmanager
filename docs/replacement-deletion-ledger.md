# Replacement Deletion Ledger

This is a living deletion ledger, not a speculative or complete source inventory. Add each temporary compatibility seam in the same commit that introduces it, with an exact deletion proof. No backward-compatibility seam survives Phase 11.

| Current path or owner | Current responsibility | Replacement phase | Delete only after |
|---|---|---:|---|
| `src/app/mod.rs` | Window, orchestration, UI, and background polling | 2–10 | `phase-10-feature-parity` passes after the file is removed, and `rg -n -e "mod app" -e "crate::app" -e "devmanager::app" src tests` returns no matches |
| `src/services/process_manager.rs` | PTY, provider, server, and process-lifecycle monolith | 3–8 | `phase-03-zero-orphan`, `phase-04-provider-conformance`, and `phase-08-browser-conformance` pass after the file is removed, and `rg -n -e "process_manager" -e "ProcessManager" src tests` returns no matches |
| `src/terminal/session.rs::TerminalSession` | In-process PTY, child-process, and canonical-grid authority | 3 | `phase-03-terminal` and `phase-03-zero-orphan` pass after the old type is removed, and `rg -n "\\bTerminalSession\\b" src tests --glob "*.rs"` returns no matches |
| `src/ai/codex_cli.rs` | Window-side Codex capability probe and launch overrides | 4 | `phase-04-provider-conformance` passes after the file is removed, and `rg -n -e "codex_cli" -e "CodexConfigOverride" src tests --glob "*.rs"` returns no matches |
| `src/ai/codex_rollout.rs` | Rollout-directory inference, tailing, and semantic reduction | 4 | `phase-04-provider-conformance` passes after the file is removed, and `rg -n -e "codex_rollout" -e "CodexRolloutReducer" -e "CodexRolloutTailer" src tests --glob "*.rs"` returns no matches |
| `src/browser/pane.rs` legacy renderer symbols (`BrowserPaneContext`, `BrowserPaneTransient`, `BrowserPaneModel`, `BrowserPaneActions`, `render_browser_pane`) | Window-owned browser pane composition and callbacks | 5, 8 | `phase-08-browser-conformance` passes after the legacy symbols are removed, and `rg -n -e "BrowserPaneContext" -e "BrowserPaneTransient" -e "BrowserPaneModel" -e "BrowserPaneActions" -e "render_browser_pane" src tests --glob "*.rs"` returns no matches |
| `src/state/` | Tab and runtime read models | 1–6 | `phase-06-task-projections` passes after the directory is removed, and `rg -n -e "mod state" -e "crate::state" -e "devmanager::state" src tests` returns no matches |
| `src/models/config.rs` (`SessionState`/`SessionTab`) | Old open-tab persistence data model | 1, 11 | `phase-11-cutover` passes, and `rg -n -e "SessionState" -e "SessionTab" src tests --glob "*.rs"` returns no matches |
| `src/persistence/mod.rs` session path/read-write | Legacy `session.json` resolution, load/save, and migration | 1, 11 | `phase-11-cutover` passes, and `rg -n -e "SESSION_FILE_NAME" -e "session_path" -e "session\\.json" src/persistence src/config src/kernel --glob "*.rs"` returns no matches |
| `src/services/session_manager.rs` | Runtime session save behavior | 1, 11 | `phase-11-cutover` passes after the file is removed, and `rg -n -e "SessionManager" -e "session_manager" src tests --glob "*.rs"` returns no matches |
| `src/remote/mod.rs` old snapshot bridge | Window-owned remote authority | 2, 9 | `phase-09-connect-realtime` passes after the old snapshot-bridge symbols are removed, and `rg -n -e "RemoteWorkspaceSnapshot" -e "current_snapshot" -e "update_snapshot" src/remote src/app tests --glob "*.rs"` returns no matches |
| `src/remote/web/bridge.rs` old bridge | Old web mutation and snapshot transport | 9 | `phase-09-connect-realtime` passes after the file is removed, and `rg -n -e "remote::web::bridge" -e "web::bridge" -e "mod bridge" src tests --glob "*.rs"` returns no matches |
| `src/sidebar/` | Old GPUI navigation | 5 | `phase-05-task-cockpit` passes after the directory is removed, and `rg -n -e "mod sidebar" -e "crate::sidebar" -e "devmanager::sidebar" src tests` returns no matches |
| `src/workspace/editor_ui.rs` | Old form and editor primitives | 5–6 | `phase-06-configuration` passes after the file is removed, and `rg -n -e "mod editor_ui" -e "editor_ui::" src tests --glob "*.rs"` returns no matches |
| `tests/legacy_loader.rs` | Old config and session migration coverage | 11 | `phase-11-cutover` passes after the file is removed, and `rg -n "legacy_loader" src tests Cargo.toml` returns no matches |
| `tests/fixtures/legacy-session.json` | Old tab-state migration fixture | 11 | `phase-11-cutover` passes after the fixture is removed, and `rg -n "legacy-session\\.json" src tests Cargo.toml` returns no matches |
| `zz-archive/tauri-react-v0.1.11` | Archived legacy desktop implementation | 11 | `phase-11-release` passes after the directory is removed, and `rg -n -e "zz-archive/tauri-react-v0\\.1\\.11" -e "tauri-react-v0\\.1\\.11" . --glob "!target/**" --glob "!.git/**" --glob "!docs/replacement-deletion-ledger.md"` returns no matches |
